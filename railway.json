{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx tsc -b --noEmit || true"
  },
  "deploy": {
    "startCommand": "npx tsx packages/marketplace/src/mcp-server.ts",
    "healthcheckPath": "/"
  }
}
